# pi-realtime Backlog Notes

Deferred polish from live OpenAI smoke testing:

- Make `/realtime stop <sessionId>` work in addition to `/realtime stop --session <sessionId>`.
- Add `/realtime prune` or `/realtime clear-stopped` to hide old stopped/error sessions.
- Add a stale-session selection probe for reload/crash recovery.

These are intentionally behind microphone input and audio UX work.
